# Python SDK (cpak)

## Installation

```bash
cpak install github.com/containerpak/sdk-python
```

The package exports `python3` and `pip3` and can be mounted as an add-on by other cpak applications.
